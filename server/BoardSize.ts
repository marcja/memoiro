export class BoardSize {
  width: number;
  height: number;
  count: number;

  constructor(width: number, height: number, count: number) {
    this.width = width;
    this.height = height;
    this.count = count;
  }
}
