{
  "name": "memoiro",
  "version": "1.0.0",
  "description": "Gather participant samples for experiment on color and memory.",
  "main": "client/index.js",
  "scripts": {
    "build": "parcel build client/index.html server/index.ts --no-minify --no-cache --no-source-maps --global Memoiro && cp appsscript.json server/entry.js dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Marc Jacobs <marcja@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "@google/clasp": "^2.3.0",
    "@types/google-apps-script": "^1.0.16",
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-inliner": "^1.0.14"
  },
  "devDependencies": {
    "@babel/core": "^7.12.3",
    "babel-preset-gas": "^2.0.4",
    "sass": "^1.27.0"
  },
  "babel": {
    "presets": [
      "gas"
    ]
  }
}
